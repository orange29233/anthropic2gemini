name = "anthropic2gemini"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 环境变量配置
# DEFAULT_MODEL: 默认 Gemini 模型（所有 Claude 模型都会映射到此模型）
[vars]
DEFAULT_MODEL = "gemini-3-flash-preview"

# 本地开发环境变量（不提交到 git）
# 使用 .dev.vars 文件存储本地开发用的 API Keys

# 自定义域名需要在 Cloudflare Dashboard 中手动配置 Worker 路由
